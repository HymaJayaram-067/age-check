apiVersion: v1
kind: Service
metadata:
  name: age-check-ollama
  namespace: age-check
spec:
  selector:
    app: age-check-ollama
  ports:
    - name: http
      port: 11434
      targetPort: 11434
  type: ClusterIP
