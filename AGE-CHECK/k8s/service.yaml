apiVersion: v1
kind: Service
metadata:
  name: age-check-api-service
  namespace: age-check
spec:
  selector:
    app: age-check-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
    nodePort: 30080
  type: NodePort
